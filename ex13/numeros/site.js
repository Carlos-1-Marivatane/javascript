let numero = window.document.querySelector("input#numero").value
let numer = Number(numero)
let selet = window.document.querySelector("select#sele")
let res = window.document.querySelector("div#res")
let valores = []

function num(n){
    if(n >= 1 && n <= 100){
        return true
    }else{
        return false
    }
}

function inlista(m, l){
    if(l.index)
}

function gerar(){
    if(num(numer) && inlista(numer, valores)){

    }
else {
    window.alert("Valor invalido ou ja encontrado na lista")
}}